namespace foundry {
namespace parse {

node root {
        std::string ns;
        rule rules[];
        terminal terminals[];
        string_literal literals[];
};

node rule {
        std::string name;
        alternative alternatives[];
};

node alternative {
        std::string name;
        group group;
};

group component {
        node {
                std::string name;
        };

        node string_literal
        {
                std::string text;
        };

        node unresolved_symbol {};

        node terminal {};
        node nonterminal
        {
                rule rule;
        };

        node group
        {
                component components[];
        };
};

const visitor;
visitor;

}
}
